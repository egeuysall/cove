export type Group = {
    ID: string
    Name: string
    CreatedBy: string
    CreatedAt: string
}

export type Links = {
    id: string
    user_id: string
    url: string
    title: string
    comment: string
    created_at: string
}